{
  "name": "visionia-server",
  "version": "2.0.0",
  "description": "VisionIA — Servidor optimizado con WebSocket + HTTP",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "tunnel": "npx localtunnel --port 3000"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "ws": "^8.16.0"
  }
}
